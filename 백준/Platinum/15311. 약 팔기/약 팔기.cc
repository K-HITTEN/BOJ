#include <iostream>
#define fastio cin.tie(0)->ios_base::sync_with_stdio(0)

using namespace std;

int main(void){
    fastio;
    int N;
    cin >> N;
    cout << 2000 <<"\n";
    for(int i = 0; i < 1000; i++){
        cout << 1000 << " ";
    }
    for(int i = 0; i < 1000; i++){
        cout << 1 << " ";
    }
}