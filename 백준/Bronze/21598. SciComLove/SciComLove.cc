#include <iostream>
#define fastio cin.tie(0)->ios_base::sync_with_stdio(0)

using namespace std;

int main(void){
    fastio;
    long long N;
    cin >> N;
    for(long long i = 0; i < N; i++){
        cout << "SciComLove\n";
    }
}