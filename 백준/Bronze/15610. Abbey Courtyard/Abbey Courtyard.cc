#include <iostream>
#include <cmath>
#define fastio cin.tie(0)->ios_base::sync_with_stdio(0)

using namespace std;

int main(void){
    fastio;
    cout << fixed;
    cout.precision(7);
    double N;
    cin >> N;
    cout << sqrt(N)*4;
}