#include <iostream>
#include <cmath>

using namespace std;

int main(void){
    cin.tie(0)->ios_base::sync_with_stdio(0);
    cout << fixed;
    cout.precision(7);
    double N;
    cin >> N;
    cout << sqrt(N)*4;
}